services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: retail-pos-frontend
    image: nongdev/retail-pos-frontend:latest
    restart: always
    networks:
      - web

networks:
  web:
    external: true
  internal_project_network:
    driver: bridge
